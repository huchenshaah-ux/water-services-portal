{
    "private": true,
    "scripts": {
        "vercel-build": "mkdir -p storage/framework/cache/data storage/framework/sessions storage/framework/views storage/logs bootstrap/cache && composer install --no-dev --optimize-autoloader --no-interaction"
    }
}
